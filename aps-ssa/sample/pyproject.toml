[project]
name = "aps-ssa-sample"
version = "0.1.0"
description = "Manually verify aps-ssa against a real APS service account"
requires-python = ">=3.11"
dependencies = [
    "aps-ssa",
    "python-dotenv>=1.0",
]

[tool.uv.sources]
aps-ssa = { path = "../" }

[tool.uv]
package = false
